function larger(a,b) {
    if (a < b) {
        console.log( b + " est le plus grand que " + a);
    }
    if (a > b) {
        console.log(a + " est le plus grand que " + b);
        
    }
}
larger(8,4)